version: 0.2

phases:
  install:
    runtime-versions:
      dotnet: 8.0

  build:
    commands:
      - dotnet restore APIsNABH.csproj
      - dotnet build APIsNABH.csproj -c Release
      - dotnet publish APIsNABH.csproj -c Release -o publish

artifacts:
  files:
    - '**/*'